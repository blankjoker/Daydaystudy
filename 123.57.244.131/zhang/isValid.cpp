

#include <stdio.h>
#include <string>

class Solution {
public:
    bool isValid(string s) {
        int len = s.size();
        if (len % 2 == 1) {
            return false;
        }
        
        for (int i = 0; i < len; i++) {

        }
    }
};


int main(int argc, char const *argv[])
{
    /* code */
    return 0;
}
